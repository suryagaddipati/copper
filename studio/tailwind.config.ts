// Tailwind CSS v4 configuration
export default {
  // In Tailwind v4, content sources are now defined via @source directives in CSS
  // or can be specified here for compatibility
  content: [
    "./src/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  darkMode: ['class', '[data-theme="dark"]'],
};