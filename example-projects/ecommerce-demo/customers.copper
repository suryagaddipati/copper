dimension: customer_id {
  expression: ${customers.customer_id} ;;
}

dimension: customer_name {
  expression: ${customers.customer_name} ;;
}

dimension: age {
  expression: ${customers.age} ;;
}

dimension: is_active {
  expression: ${customers.is_active} ;;
}
